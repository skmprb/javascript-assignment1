var h = parseInt(prompt("enter the height of the triangle : "));
var b = parseInt(prompt("enter the base of the triangle : "));

var area = (h * b)/2;

console.log(" the area of the triangle is : "+area);